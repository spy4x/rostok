.volumes
dist
build
.idea
.DS_Store
node_modules
*.passwd

# Configs
.env
.env.*
*.env
!.env.example
!*.env.example
!.env.root.example

# Age encryption keys (never commit!)
.age/
key.txt

.playwright-mcp/
todos.txt

# Rendered secrets (do not commit)
stacks/*/config.toml
stacks/*/*.rendered
stacks/*/mta-sts/html/.well-known/mta-sts.txt
*.sops-backup

# Claude Code local settings (machine-specific, do not commit)
.claude/settings.local.json
