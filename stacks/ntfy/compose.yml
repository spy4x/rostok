name: ${PROJECT}

networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  # ntfy - Notification service for alerts
  # Can be used for mail server alerts and backup notifications
  ntfy:
    container_name: hl-ntfy
    image: binwiederhier/ntfy:latest
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:80/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    command:
      - serve
    environment:
      - TZ=${TIMEZONE:-UTC}
      - NTFY_BASE_URL=https://${NTFY_SUBDOMAIN}.${DOMAIN}
      - NTFY_UPSTREAM_BASE_URL=https://ntfy.sh
      - NTFY_CACHE_FILE=/var/lib/ntfy/cache.db
      - NTFY_AUTH_FILE=/var/lib/ntfy/auth.db
      - NTFY_ATTACHMENT_CACHE_DIR=/var/lib/ntfy/attachments
      - NTFY_AUTH_DEFAULT_ACCESS=deny-all
      - NTFY_BEHIND_PROXY=true
      - NTFY_ENABLE_SIGNUP=false
      - NTFY_ENABLE_LOGIN=true
    user: "${PUID:-1000}:${PGID:-1000}"
    volumes:
      - ${VOLUMES_PATH}/ntfy/cache:/var/lib/ntfy:z
      - ${VOLUMES_PATH}/ntfy/etc:/etc/ntfy:z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ${NTFY_MEM_LIMIT:-128M}
          cpus: ${NTFY_CPU_LIMIT:-0.2}
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-ntfy.loadbalancer.server.port=80"
      - "traefik.http.routers.hl-ntfy.rule=Host(`${NTFY_SUBDOMAIN}.${DOMAIN}`)"
      - "traefik.http.routers.hl-ntfy.middlewares=robots-deny@file"
      - "traefik.http.routers.hl-ntfy.entrypoints=websecure"
      - "traefik.http.routers.hl-ntfy.tls=true"
      - "traefik.http.routers.hl-ntfy.tls.certresolver=myresolver"
