networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  # MeTube is a web GUI for youtube-dl with playlist support.
  # It allows you to download videos from YouTube and other supported sites.
  metube:
    container_name: hl-metube
    image: ghcr.io/alexta69/metube
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8081/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TIMEZONE}
      - DOWNLOAD_DIR=/downloads/videos
      - AUDIO_DOWNLOAD_DIR=/downloads/music
      - OUTPUT_TEMPLATE=%(uploader,channel)s/%(title)s.%(ext)s
      - OUTPUT_TEMPLATE_PLAYLIST=%(playlist,uploader,channel)s/%(title)s.%(ext)s
    volumes:
      - ${PATH_VIDEOS}:/downloads/videos:z
      - ${PATH_MUSIC}:/downloads/music:z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-metube.loadbalancer.server.port=8081"
      - "traefik.http.routers.hl-metube.rule=Host(`metube.${DOMAIN}`)"
      - "traefik.http.routers.hl-metube.entrypoints=websecure"
      - "traefik.http.routers.hl-metube.tls=true"
      - "traefik.http.routers.hl-metube.tls.certresolver=myresolver"
      - "traefik.http.routers.hl-metube.middlewares=authelia@file,robots-deny@file"
