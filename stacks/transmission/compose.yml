networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  # Transmission is a BitTorrent client with a web interface.
  # It is a lightweight, works on the server in background and has zero configuration.
  transmission:
    image: linuxserver/transmission
    container_name: hl-transmission
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TIMEZONE}
    volumes:
      - ${VOLUMES_PATH}/transmission:/config:z
      - ${PATH_MOVIES}:/downloads/movies:z
      - ${PATH_SERIES}:/downloads/series:z
      - ${PATH_MUSIC}:/downloads/music:z
      - ${PATH_BOOKS}:/downloads/books:z
      - ${PATH_OTHER}:/downloads/other:z
    ports:
      - 51413:51413 # Default port for torrent traffic
      - 51413:51413/udp # Default port for DHT
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2048M
          cpus: "1"
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-transmission.loadbalancer.server.port=9091"
      - "traefik.http.routers.hl-transmission.rule=Host(`torrents.${DOMAIN}`)"
      - "traefik.http.routers.hl-transmission.entrypoints=websecure"
      - "traefik.http.routers.hl-transmission.tls=true"
      - "traefik.http.routers.hl-transmission.tls.certresolver=myresolver"
      # SSO via Authelia (forward auth).
      # See stacks/traefik/dynamic.yml for the middleware definition.
      - "traefik.http.routers.hl-transmission.middlewares=authelia@file,robots-deny@file"
