# OpenCode Web

**DECOMMISSIONED** — OpenCode Web now runs as a standalone systemd service outside Docker.

## Status

This compose file is kept for reference only and is not deployed. The `decommissioned` profile prevents accidental launch.

## Migration

See `docs/opencode-web.md` for current deployment instructions as a systemd service.
