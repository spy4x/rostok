name: ${PROJECT}
networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  # Loki - Log aggregation (receives from Promtail, serves to Grafana)
  loki:
    container_name: hl-loki
    image: grafana/loki:3.7.3
    user: "${PUID:-1000}:${PGID:-1000}"
    volumes:
      - ${VOLUMES_PATH}/monitoring/loki:/loki:z
    command: -config.file=/etc/loki/local-config.yaml
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.3"
    networks:
      - default

  # Promtail - Log collector (reads container logs, ships to Loki)
  promtail:
    container_name: hl-promtail
    image: grafana/promtail:3.6.11
    volumes:
      - ${PATH_APPS}/configs/monitoring/promtail.yml:/etc/promtail/config.yml:ro,z
      - /var/log:/var/log:ro,z
      - /var/lib/docker/containers:/var/lib/docker/containers:ro,z
      - /var/run/docker.sock:/var/run/docker.sock:ro,z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.2"
    group_add:
      - "${DOCKER_GROUP_ID:-990}"
    security_opt:
      - label=type:container_runtime_t
    networks:
      - default

  # Node Exporter - Host-level metrics (CPU, memory, disk, network)
  node-exporter:
    container_name: hl-node-exporter
    image: prom/node-exporter:v1.11.1
    volumes:
      - /proc:/host/proc:ro,z
      - /sys:/host/sys:ro,z
      - /:/rootfs:ro,z
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--path.rootfs=/rootfs"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: "0.1"
    networks:
      - default

  # Prometheus - Metrics storage & query engine
  prometheus:
    container_name: hl-prometheus
    image: prom/prometheus:v3.13.0
    volumes:
      - ${VOLUMES_PATH}/monitoring/prometheus:/prometheus:z
      - ${PATH_APPS}/configs/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro,z
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    networks:
      - default

  # cAdvisor - Container-level metrics (resource usage per container)
  # NOTE: requires seccomp=unconfined for syscalls needed to read container stats
  cadvisor:
    container_name: hl-cadvisor
    image: gcr.io/cadvisor/cadvisor:v0.49.2
    volumes:
      - /:/rootfs:ro,z
      - /var/run:/var/run:ro,z
      - /sys:/sys:ro,z
      - /var/lib/docker:/var/lib/docker:ro,z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: "0.3"
    group_add:
      - "${DOCKER_GROUP_ID:-990}"
    security_opt:
      - seccomp=unconfined
      - label=type:container_runtime_t
    networks:
      - default
