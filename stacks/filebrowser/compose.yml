networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  filebrowser:
    container_name: hl-filebrowser
    image: filebrowser/filebrowser:v2.63.17
    user: "${PUID:-1000}:${PGID:-1000}"
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TIMEZONE}
    volumes:
      - ${VOLUMES_PATH}/filebrowser:/database:z
      - ${PATH_MEDIA}:/srv/media:z
      - ${PATH_VIDEOS}:/srv/videos:z
      - ${PATH_MUSIC}:/srv/music:z
      - ${PATH_BOOKS}:/srv/books:z
      - ${PATH_SYNC}:/srv/sync:z
      - ${PATH_MOVIES}:/srv/movies:z
      - ${PATH_SERIES}:/srv/series:z
      - ${PATH_OTHER}:/srv/other:z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-filebrowser.loadbalancer.server.port=80"
      - "traefik.http.routers.hl-filebrowser.rule=Host(`files.${DOMAIN}`)"
      - "traefik.http.routers.hl-filebrowser.middlewares=robots-deny@file"
      - "traefik.http.routers.hl-filebrowser.entrypoints=websecure"
      - "traefik.http.routers.hl-filebrowser.tls=true"
      - "traefik.http.routers.hl-filebrowser.tls.certresolver=myresolver"
