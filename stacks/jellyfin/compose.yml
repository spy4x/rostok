networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  jellyfin:
    container_name: hl-jellyfin
    image: jellyfin/jellyfin
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8096/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
      - ${VOLUMES_PATH}/jellyfin:/config:z
      - ${VOLUMES_PATH}/jellyfin-cache:/cache:z
      - ${PATH_MEDIA}:/media:z
      - ${PATH_VIDEOS}:/videos:z
      - ${PATH_MUSIC}:/music:z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 8192M
          cpus: "3"
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu
                - video
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.hl-jellyfin.loadbalancer.server.port=8096"
      - "traefik.http.routers.hl-jellyfin.rule=Host(`movies.${DOMAIN}`)"
      - "traefik.http.routers.hl-jellyfin.middlewares=robots-deny@file"
      - "traefik.http.routers.hl-jellyfin.entrypoints=websecure"
      - "traefik.http.routers.hl-jellyfin.tls=true"
      - "traefik.http.routers.hl-jellyfin.tls.certresolver=myresolver"
