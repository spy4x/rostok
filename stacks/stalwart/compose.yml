name: ${PROJECT}

networks:
  proxy:
    external: true
  default:
    external: true
    name: proxy

services:
  # Stalwart Mail Server - Modern mail server with JMAP support
  stalwart:
    container_name: hl-stalwart
    image: stalwartlabs/stalwart:latest
    ports:
      - "25:25"
      - "465:465"
      - "587:587"
      - "993:993"
      - "172.18.0.1:8080:8080"
    environment:
      - TZ=${TIMEZONE:-UTC}
      - STALWART_RECOVERY_ADMIN=admin:${STALWART_ADMIN_PASSWORD}
    volumes:
      - ${VOLUMES_PATH}/stalwart/data:/opt/stalwart-mail/data:z
      - ${VOLUMES_PATH}/stalwart/config:/etc/stalwart:z
      - ${VOLUMES_PATH}/stalwart/lib:/var/lib/stalwart:z
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: ${STALWART_MEM_LIMIT:-512M}
          cpus: ${STALWART_CPU_LIMIT:-0.5}
    security_opt:
      - no-new-privileges:true
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      # Shared backend via host gateway (bypasses Docker inter-container issue)
      - "traefik.http.services.hl-stalwart.loadbalancer.server.url=http://172.18.0.1:8080"
      # Admin router on stalwart.DOMAIN → /admin
      - "traefik.http.routers.hl-stalwart-admin.rule=Host(`${STALWART_SUBDOMAIN:-stalwart}.${DOMAIN}`)"
      - "traefik.http.routers.hl-stalwart-admin.entrypoints=websecure"
      - "traefik.http.routers.hl-stalwart-admin.tls=true"
      - "traefik.http.routers.hl-stalwart-admin.tls.certresolver=myresolver"
      - "traefik.http.routers.hl-stalwart-admin.service=hl-stalwart"
      - "traefik.http.routers.hl-stalwart-admin.middlewares=hl-stalwart-root-redirect,robots-deny@file"
      - "traefik.http.middlewares.hl-stalwart-root-redirect.redirectregex.regex=^(https?://[^/]+)/?$$"
      - "traefik.http.middlewares.hl-stalwart-root-redirect.redirectregex.replacement=$${1}/admin"
      - "traefik.http.middlewares.hl-stalwart-root-redirect.redirectregex.permanent=true"
      # Webmail router on webmail.DOMAIN → /account
      - "traefik.http.routers.hl-stalwart-webmail.rule=Host(`webmail.${DOMAIN}`)"
      - "traefik.http.routers.hl-stalwart-webmail.entrypoints=websecure"
      - "traefik.http.routers.hl-stalwart-webmail.tls=true"
      - "traefik.http.routers.hl-stalwart-webmail.tls.certresolver=myresolver"
      - "traefik.http.routers.hl-stalwart-webmail.service=hl-stalwart"
      - "traefik.http.routers.hl-stalwart-webmail.middlewares=hl-stalwart-webmail-redirect,robots-deny@file"
      - "traefik.http.middlewares.hl-stalwart-webmail-redirect.redirectregex.regex=^(https?://[^/]+)/?$$"
      - "traefik.http.middlewares.hl-stalwart-webmail-redirect.redirectregex.replacement=$${1}/account"
      - "traefik.http.middlewares.hl-stalwart-webmail-redirect.redirectregex.permanent=true"
