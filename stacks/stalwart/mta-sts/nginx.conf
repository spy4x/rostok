server {
    listen 8081;
    server_name _;
    
    location /.well-known/mta-sts.txt {
        alias /usr/share/nginx/html/.well-known/mta-sts.txt;
        default_type text/plain;
        add_header Content-Type text/plain;
    }
    
    location / {
        return 404;
    }
}
